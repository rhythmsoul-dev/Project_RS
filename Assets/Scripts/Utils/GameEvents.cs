using System;
using UnityEngine;

public static class GameEvents
{
    // public static event Action<string> OnMeetNPC;
    // public static event Action OnPlayerFirstDeath;
    // public static event Action<string> OnBossCleared;
    //
    // public static void MeetNPC(string npcId) => OnMeetNPC?.Invoke(npcId);
    // public static void PlayerFirstDied() => OnPlayerFirstDeath?.Invoke();
    // public static void BossCleared(string bossId) => OnBossCleared?.Invoke(bossId);
}